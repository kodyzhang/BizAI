{{!-- SAP CDS View → TypeScript entity interface + list alias --}}
// Auto-generated from SAP CDS View: {{name}}
// {{#if description}}{{description}}{{else}}(no description){{/if}}
// Generated at: {{isoDate}}

export interface {{pascalCase name}} {
{{#each fields}}
  {{#if description}}/** {{description}} */
  {{/if}}{{camelCase name}}: {{abapToTs type}};
{{/each}}
}

export type {{pascalCase name}}List = {{pascalCase name}}[];

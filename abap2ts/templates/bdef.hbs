{{!-- RAP Business Object Definition (BDEF) → TypeScript entity + action types --}}
// Auto-generated from SAP RAP BDEF: {{name}}
// {{#if description}}{{description}}{{else}}(no description){{/if}}
// Generated at: {{isoDate}}

export interface {{pascalCase name}} {
{{#each fields}}
  {{#if description}}/** {{description}} */
  {{/if}}{{camelCase name}}: {{abapToTs type}};
{{/each}}
}

export interface {{pascalCase name}}Key {
  {{#each fields}}{{#if required}}{{camelCase name}}: {{abapToTs type}};
  {{/if}}{{/each}}
}

{{#if methods}}
export interface {{pascalCase name}}Actions {
{{#each methods}}
  {{camelCase name}}(
    {{#if importing}}params: {
      {{#each importing}}{{camelCase name}}: {{abapToTs type}};
      {{/each}}
    }{{/if}}
  ): {{#if returning}}Promise<{{abapToTs returning.type}}>{{else}}Promise<void>{{/if}};
{{/each}}
}
{{/if}}

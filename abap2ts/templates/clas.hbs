{{!-- ABAP Class (CLAS) → TypeScript interface + method signatures --}}
// Auto-generated from SAP ABAP Class: {{name}}
// {{#if description}}{{description}}{{else}}(no description){{/if}}
// Generated at: {{isoDate}}

{{#if fields}}
export interface {{pascalCase name}}Attributes {
{{#each fields}}
  {{#if description}}/** {{description}} */
  {{/if}}{{camelCase name}}: {{abapToTs type}};
{{/each}}
}

{{/if}}
export interface {{pascalCase name}} {
{{#each methods}}
  /**{{#if description}} {{description}}{{/if}} */
  {{camelCase name}}(
    {{#if importing}}params: {
      {{#each importing}}{{camelCase name}}: {{abapToTs type}};{{#if description}} // {{description}}{{/if}}
      {{/each}}
    }{{/if}}
  ): {{#if returning}}Promise<{{abapToTs returning.type}}>{{else}}Promise<void>{{/if}};

{{/each}}
}
